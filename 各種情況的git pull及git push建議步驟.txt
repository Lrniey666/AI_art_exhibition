cd C:/python-10

git pull建議步驟:

git pull


(壞掉時能修復)強制覆蓋本地程式git pull步驟:

git stash

git pull origin master//需要先處理衝突

git stash pop


(壞掉時能無法修復)強制覆蓋本地程式git pull步驟:

git push -f origin master

git pull


git push建議步驟:

git status//哪些文件已經被修改過

git add .//添加你想要提交的改變(全部) git add ...py 添加你想要提交的改變(...py)

git commit -m "資料庫增加了2016到2021(因為這是最新的)的全台家戶收入，不過有些離島的資料TDX就有缺失了"

git push//沒成功逝世以下指令

先合併:
git pull --rebase

強制取代:
git config --global push.default simple


高速開網站:

cd C:/python-10 && python manage.py runserver




